info:
  name: Полная приёмка кодов (POST)
  type: http
  seq: 21
  tags:
    - Отгрузки и приёмка

http:
  method: POST
  url: "{{baseUrl}}/v3/ships/take_all"
  headers:
    - name: Token
      value: "{{apiKey}}"
      description: Токен авторизации, возвращаемый методом /auth
  auth:
    type: apikey
    key: Token
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Результат запуска полной приёмки кодов отгрузки
    request:
      url: "{{baseUrl}}/v3/ships/take_all"
      method: POST
      headers:
        - name: Token
          value: "{{apiKey}}"
          description: Токен авторизации, возвращаемый методом /auth
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json; charset=utf-8
      body:
        type: text
        data: |-
          {
            "detail": "Приемка начата",
            "ship_id": 2000040045
          }
  - name: default Response
    description: Ошибка запроса. Коды ошибок и их описание приведены в таблице 5 (раздел 5 «Коды ошибок и их описание») Ошибка выполнения запроса (HTTP 4XX). Тело ответа — единая схема ErrorResponse.
    request:
      url: "{{baseUrl}}/v3/ships/take_all"
      method: POST
      headers:
        - name: Token
          value: "{{apiKey}}"
          description: Токен авторизации, возвращаемый методом /auth
    response:
      headers:
        - name: Content-Type
          value: application/json; charset=utf-8
      body:
        type: text
        data: |-
          {
            "details": [
              "Field ... must be ..."
            ],
            "error": 400,
            "message": "Input parameters validation error"
          }

docs: "Метод «Полная приёмка кодов» (п. 3.7.12.2, Представление Б, версия v3). Выполняет полную приёмку всех кодов отгрузки по её номеру (ID отгрузки, полученному из метода /v3/ships/list, см. п. 3.7.5.2). Представление А метода: POST /ships/takeAll."
