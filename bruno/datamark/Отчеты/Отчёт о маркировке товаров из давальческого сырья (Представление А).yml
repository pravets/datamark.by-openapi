info:
  name: Отчёт о маркировке товаров из давальческого сырья (Представление А)
  type: http
  seq: 10
  tags:
    - Отчеты

http:
  method: POST
  url: "{{baseUrl}}/v2/reports/addMarkTrans"
  headers:
    - name: Token
      value: "{{apiKey}}"
      description: Токен авторизации, возвращаемый методом /auth
  auth:
    type: apikey
    key: Token
    value: "{{apiKey}}"
    placement: header

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5

examples:
  - name: 200 Response
    description: Успешное выполнение запроса
    request:
      url: "{{baseUrl}}/v2/reports/addMarkTrans"
      method: POST
      headers:
        - name: Token
          value: "{{apiKey}}"
          description: Токен авторизации, возвращаемый методом /auth
    response:
      status: 200
      statusText: OK
      headers:
        - name: Content-Type
          value: application/json; charset=utf-8
      body:
        type: text
        data: |-
          {
            "report_id": "d9ba0436-70ee-449f-8c53-5a3624a32b9f"
          }
  - name: default Response
    description: Ошибка запроса. Коды ошибок и их описание приведены в таблице 5 (раздел 5 «Коды ошибок и их описание») Ошибка выполнения запроса (HTTP 4XX). Тело ответа — единая схема ErrorResponse.
    request:
      url: "{{baseUrl}}/v2/reports/addMarkTrans"
      method: POST
      headers:
        - name: Token
          value: "{{apiKey}}"
          description: Токен авторизации, возвращаемый методом /auth
    response:
      headers:
        - name: Content-Type
          value: application/json; charset=utf-8
      body:
        type: text
        data: |-
          {
            "details": [
              "Field ... must be ..."
            ],
            "error": 400,
            "message": "Input parameters validation error"
          }

docs: "Метод «Отчёт о маркировке товаров из давальческого сырья» (п. 3.5.12). Для данного пути применяется Представление А (п. 3.5.12.1, API версии v2). Альтернативное Представление Б (п. 3.5.12.2, API версии v3) доступно по пути /v3/reports/addMarkTrans и допускает только КМ российского образца (label_type = 7). Явная привязка товарных групп к представлениям в документе не указана: в примерах обоих представлений использована товарная группа milk."
